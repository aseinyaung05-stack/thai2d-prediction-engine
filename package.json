{
  "name": "thai2d-prediction-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Thai 2D historical-data statistical analysis & prediction platform (educational/research). Monorepo: Next.js web, Express API, Python FastAPI prediction service.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:web": "npm run dev --workspace apps/web",
    "dev:api": "npm run dev --workspace apps/api",
    "build:web": "npm run build --workspace apps/web",
    "build:api": "npm run build --workspace apps/api",
    "build:shared": "npm run build --workspace packages/shared",
    "db:push": "npm run db:push --workspace apps/api",
    "db:generate": "npm run db:generate --workspace apps/api",
    "test": "npm run test --workspace packages/shared && npm run test --workspace apps/api",
    "test:python": "cd services/prediction && python -m pytest -q",
    "typecheck": "npm run typecheck --workspace packages/shared && npm run typecheck --workspace apps/api && npm run typecheck --workspace apps/web",
    "first-run": "powershell -ExecutionPolicy Bypass -File scripts/first-run.ps1"
  }
}

[pytest]
testpaths = tests
addopts = -q --tb=short -m "not integration"
markers =
    integration: requires external network/API access (run explicitly with -m integration)
filterwarnings =
    ignore::DeprecationWarning
